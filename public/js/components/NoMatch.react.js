var React = require('react');

// Flux product view
var NoMatch = React.createClass({
	  // Render product View
	  render: function() {
		return (
		  <div className="page">
			
		  </div>
		);
	  }
});

module.exports = NoMatch;